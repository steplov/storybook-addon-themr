import {
  PROGRESS_BAR,
  BUTTON
} from 'react-toolbox/lib/identifiers';
import progressBar from './progress-bar.pcss';
import button from './button.pcss';

const theme = {
  [PROGRESS_BAR]: progressBar,
  [BUTTON]: button
};

export default theme;
